# Minimap Idle Constructor Blips

The normal "idle builders" interface is too far out of my line of sight usually, so I often miss the notication. Using this widget it adds yellow blips that go on and off to get your attention as an extra way to see where your idle builders are.

